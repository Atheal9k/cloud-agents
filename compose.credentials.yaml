services:
  controller:
    volumes:
      - type: bind
        source: ${T3CODE_CREDENTIALS_DIR:?Set T3CODE_CREDENTIALS_DIR to a protected host directory}
        target: /run/t3-credentials
        read_only: true
